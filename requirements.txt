numpy==2.2.2
opencv-python==4.11.0.86
Pillow==11.1.0
piexif==1.1.3
torch==2.6.0   # For macOS, you might need the CPU-only version if GPU support isn't available
torchvision==0.21.0
rawpy==0.24.0
PyQt5==5.15.11
pillow-heif==0.21.0
retinaface-pytorch==0.0.7
pydng==1.1.2        # Optional, only if using DNG output
pytest==8.3.3        # Optional, for testing
